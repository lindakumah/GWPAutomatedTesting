name: 'Cypress Tests'

on:
  push:
    branches:
      - testing
  schedule:
    - cron: '* 09 * * 1,3'
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        containers: [1, 2]
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Cypress run
        uses: cypress-io/github-action@v2
        with:
         start: yarn start
         wait-on: 'https://gds-test.amalitech-dev.net'
         wait-on-timeout: 120
         record: true
         parallel: true
         spec: cypress/e2e/*
        env:
         CYPRESS_RECORD_KEY: ${{ secrets.CYPRESS_RECORD_KEY }}
         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

